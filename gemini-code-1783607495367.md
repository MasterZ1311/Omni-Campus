You are an expert full-stack TypeScript engineer. Help me extend the existing "Omni-Campus" Smart Campus Resource Management application by implementing three new feature verticals across both the backend and frontend.

### 1. Architectural Baseline
The existing application follows this tech stack:
- Backend: Node.js, Express, TypeScript, Prisma ORM, Redis, Passport.js.
- Frontend: React, TypeScript, Tailwind CSS, Vite.
Maintain the modular architecture by updating the Prisma schema, creating isolated services, extending controllers, and mapping matching components in the React frontend.

---

### 2. Implementation Specifications

#### Feature Track A: Staff Transportation & Vehicle Management
- **Database Schema Updates:** Add a `Vehicle` model linked to a `VehicleSchedule` and an on-demand `TransportRequest` model. Fields should track vehicle type, capacity, current status (Active, Maintenance, Out of Service), route/stops, schedule timings, and assigned driver.
- **Backend Services & API endpoints:**
  - `GET /api/transport/vehicles`: Fetch available vehicles and real-time seat availability.
  - `GET /api/transport/schedules`: Retrieve fixed route timetables.
  - `POST /api/transport/request`: Allow authorized staff to request on-demand dispatch, calculating overlapping routes or scheduling dynamically based on vehicle state.
- **Frontend UI Integration:**
  - Create a `TransportHub` section inside `ResourceDiscovery.tsx`.
  - Design an interactive schedule view component with a filtering system for routes and a button modal to handle "Request On-Demand Transport".

#### Feature Track B: Vacant Classroom Discovery & Booking
- **Database Schema Updates:** Leverage or extend the existing `Resource` model by adding a type or category for `CLASSROOM`. Include attributes for seating capacity, available presentation equipment, and real-time operational status.
- **Backend Services & API endpoints:**
  - Integrate with `availability.service.ts` to compute free slots dynamically against active class timetables.
  - `GET /api/resources/classrooms/vacant`: List currently empty or soon-to-be-vacant classrooms.
  - `POST /api/bookings`: Enable immediate walk-in reservations or peer-use requests.
- **Frontend UI Integration:**
  - Add a dedicated "Classroom Finder" tab in the frontend workspace.
  - Build a grid view detailing current vacancy countdown timers (e.g., "Vacant for the next 1h 45m") paired with one-click quick booking functionality.

#### Feature Track C: Technical Staff Dispatch & Allocation
- **Database Schema Updates:** Add a `StaffProfile` model tracking roles (e.g., Lab Assistant, IT Support, Exam Invigilator) and a `StaffAssignment` model documenting real-time tasks.
- **Backend Services & API endpoints:**
  - `POST /api/admin/assignments`: Allow administrators to dispatch technical staff to specific campus points of need (Exams, active Laboratories, Server issues).
  - Track staff availability status dynamically (Available, Assigned, Break).
  - Use the existing `notification.service.ts` framework to broadcast instant alerts to dispatched staff members upon assignment change.
- **Frontend UI Integration:**
  - Extend `AdminPanel.tsx` with a "Staff Allocation Control Panel" showcasing a live roster of technicians alongside dragging or dropdown dispatching functions.
  - Update the user `Dashboard.tsx` to display an assignment itinerary view for staff accounts flagged with technician roles.

---

### 3. Step-by-Step Instructions
1. Output the precise additions required for `backend/prisma/schema.prisma`.
2. Generate the necessary backend TypeScript controller and service files, ensuring appropriate usage of database models and transaction controls.
3. Provide the Express routing index hookups.
4. Provide the updated React pages and components, fully styled with Tailwind CSS, ensuring strict TypeScript compilation safety throughout.